# Cleaned Data Directory

This folder stores intermediate cleaned datasets after duplicate removal,
missing-value handling, and standardisation but before feature engineering.

Files are generated by running `python scripts/run_pipeline.py`.
